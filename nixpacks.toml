[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "echo 'Installing dependencies handled by build script...'"
]

[phases.build]
cmds = [
  "chmod +x railway-build.sh",
  "./railway-build.sh"
]

[start]
cmd = "cd frontend-next && npm run start"