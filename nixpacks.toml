[phases.setup]
nixPkgs = ["nodejs-20_x", "npm"]

[phases.install]
cmds = ["echo 'Skipping install phase - using build script instead'"]

[phases.build]
cmds = ["./railway-build.sh"]

[start]
cmd = "cd backend && npm start"
